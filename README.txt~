epoll多线程模式，区别于epoll perthread模式.使用边界触发模式，一个线程监听在epoll上。
当套接字从非激活变到激活态时，如果套接字上有用户提交的未被处理的IO请求时，将此请求
提交到一个队列中。多个工作线程可等待在队列上提取IO请求并处理请求.
﻿epoll多线程模式，模仿IOCP设计,一个线程在epoll上监听，一组工作线程等待在完成队列上。
用户发出IO请求时，如果当前IO处于激活态，则直接完成IO请求并返回结果，否则，请求将被缓存。
当epoll发现套接口从非激活态变成激活态时，如果有未被处理的IO请求，则弹出一个请求执行，并
将结果提交到完成队列中，供工作线程提取。
